# Test dependencies — install alongside requirements.txt
# pip install -r requirements.txt -r requirements-test.txt

pytest>=7.4.0
pytest-cov>=4.1.0         # coverage reports
pytest-mock>=3.12.0       # cleaner mock fixtures via mocker
responses>=0.24.0         # HTTP request mocking for requests library
pyspark>=3.5.0            # local Spark session for loader tests
